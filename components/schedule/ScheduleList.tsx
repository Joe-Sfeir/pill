"use client";
import {useMemo,useState} from "react";import Link from "next/link";import {classes,instructors,sessions} from "@/content/data";
export function ScheduleList({limit}:{limit?:number}){const [classId,setClass]=useState("");const [instructorId,setInstructor]=useState("");const [first,setFirst]=useState(false);const [available,setAvailable]=useState(false);const list=useMemo(()=>sessions.filter(s=>(!classId||s.classId===classId)&&(!instructorId||s.instructorId===instructorId)&&(!first||s.firstTimerEligible)&&(!available||s.status==="Available")).slice(0,limit),[classId,instructorId,first,available,limit]);return <div><p className="placeholder"><strong>Demonstration schedule:</strong> dates and statuses below are mock content, not live availability or capacity.</p>{!limit&&<div className="filters" aria-label="Schedule filters"><div className="field"><label htmlFor="class-filter">Class</label><select id="class-filter" value={classId} onChange={e=>setClass(e.target.value)}><option value="">All classes</option>{classes.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div><div className="field"><label htmlFor="instructor-filter">Instructor</label><select id="instructor-filter" value={instructorId} onChange={e=>setInstructor(e.target.value)}><option value="">All instructors</option>{instructors.map(i=><option key={i.id} value={i.id}>{i.name}</option>)}</select></div><label><input type="checkbox" checked={first} onChange={e=>setFirst(e.target.checked)}/> First-timer suitable</label><label><input type="checkbox" checked={available} onChange={e=>setAvailable(e.target.checked)}/> Available only</label><button className="btn outline" onClick={()=>{setClass("");setInstructor("");setFirst(false);setAvailable(false)}}>Clear filters</button></div>}<div aria-live="polite" className="technical">{list.length} demonstration sessions</div>{list.length===0?<p className="section">No sessions match these filters. Clear filters or contact the studio.</p>:list.map(s=>{const c=classes.find(x=>x.id===s.classId)!;const i=instructors.find(x=>x.id===s.instructorId)!;return <article className="schedule-row" key={s.id}><time dateTime={`${s.date}T${s.startTime}`}>{new Intl.DateTimeFormat("en-LB",{weekday:"short",month:"short",day:"numeric"}).format(new Date(`${s.date}T12:00:00`))}<br/><strong>{s.startTime}</strong><br/><a className="technical" href={`/api/calendar/${s.id}`}>Add to calendar</a></time><div><Link href={`/classes/${c.slug}`}><strong>{c.name}</strong></Link><div className="technical">{c.duration} min · {c.intensity}</div></div><Link href={`/instructors/${i.slug}`}>{i.name}</Link><span className="tag">{s.status}</span>{s.status==="Waitlist"?<Link className="btn outline" href={`/contact?session=${s.id}`}>Join waitlist</Link>:s.status!=="Cancelled"?<Link className="btn" href={`/book?session=${s.id}`}>Request</Link>:<span>Unavailable</span>}</article>})}</div>}
