"use client";
import {useEffect,useState} from "react";import {studio} from "@/config/studio";
type Pref={essential:true;analytics:boolean;marketing:boolean};const base:Pref={essential:true,analytics:false,marketing:false};
export function CookieConsent(){const [visible,setVisible]=useState(false);const [custom,setCustom]=useState(false);const [pref,setPref]=useState<Pref>(base);useEffect(()=>{const timer=window.setTimeout(()=>setVisible(!localStorage.getItem(studio.cookie.storageKey)),0);return()=>window.clearTimeout(timer)},[]);const save=(p:Pref)=>{localStorage.setItem(studio.cookie.storageKey,JSON.stringify({...p,version:studio.cookie.version}));setVisible(false);setCustom(false);window.dispatchEvent(new CustomEvent("forme-consent-change",{detail:p}))};useEffect(()=>{const fn=()=>{setVisible(true);setCustom(true)};window.addEventListener("forme-open-cookie-settings",fn);return()=>window.removeEventListener("forme-open-cookie-settings",fn)},[]);if(!visible)return null;return <>{custom?<div className="dialog-backdrop" role="presentation"><div className="dialog" role="dialog" aria-modal="true" aria-labelledby="cookie-title"><h2 id="cookie-title">Cookie preferences</h2><p>Essential storage keeps your choices. Analytics and marketing remain off unless you choose them.</p><label><input type="checkbox" checked disabled/> Essential (always on)</label><br/><label><input type="checkbox" checked={pref.analytics} onChange={e=>setPref({...pref,analytics:e.target.checked})}/> Analytics</label><br/><label><input type="checkbox" checked={pref.marketing} onChange={e=>setPref({...pref,marketing:e.target.checked})}/> Marketing</label><div className="actions"><button className="btn" onClick={()=>save(pref)}>Save preferences</button><button className="btn outline" onClick={()=>save(base)}>Reject non-essential</button></div></div></div>:<aside className="cookie-banner" aria-label="Cookie notice"><h2>Your privacy, in your control.</h2><p>We use essential storage only by default. Optional analytics and marketing require your permission.</p><div className="actions"><button className="btn" onClick={()=>save({essential:true,analytics:true,marketing:true})}>Accept all</button><button className="btn outline" onClick={()=>save(base)}>Reject non-essential</button><button className="btn outline" onClick={()=>setCustom(true)}>Customize</button></div></aside>}</>}
export function CookieButton(){return <button onClick={()=>window.dispatchEvent(new Event("forme-open-cookie-settings"))}>Cookie preferences</button>}
