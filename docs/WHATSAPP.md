# WhatsApp

Set `NEXT_PUBLIC_WHATSAPP_NUMBER` in international digits. Utilities remove punctuation and URL-encode approved general, first-session, membership and schedule messages. Sensitive or health information is never interpolated. Links use a new tab with `noopener noreferrer`; use phone/email if WhatsApp is unavailable.
