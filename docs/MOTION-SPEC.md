# Motion specification

Motion explains controlled tension rather than decorating the page. The tension experience changes crop and progress state as each method stage becomes active. Hover scale is restrained and never required for comprehension.

Keyboard focus exposes the same active state. Under `prefers-reduced-motion: reduce`, smooth scrolling, sticky choreography, transforms, animations, and transitions collapse to an immediate static presentation.
