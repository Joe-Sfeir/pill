# Setup

Use Node 20+, `npm install`, copy `.env.example` to `.env`, keep `MOCK_DATA_MODE=true`, and run `npm run dev`. Validate with lint, tests, and build before release. Never create or commit `.env.local` for this project.
