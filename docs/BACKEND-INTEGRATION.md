# Backend integration

Implement the interfaces in `lib/repositories/interfaces.ts`, assemble them server-side, and leave components provider-agnostic. Supported future adapters include Supabase, PostgreSQL/Prisma, Firebase, REST, GraphQL, scheduling platforms, Google Calendar, email and SMS. The optional Prisma example is isolated and never migrated automatically.
