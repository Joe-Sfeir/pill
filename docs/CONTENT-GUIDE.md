# Content guide

Replace bracketed placeholders and mock records only with approved facts. Avoid medical outcomes, weight-loss language, fake urgency, fake reviews, unsupported credentials, official affiliations, or trademarked equipment terminology. Keep business facts in `config/studio.ts` and domain content in `content/data.ts`.
