# Schedule data

Every session includes date/time, `Asia/Beirut`, status, waitlist state, booking URL, and first-timer eligibility. Current records are demonstration content. Capacity/booked values remain `null`. Individual `.ics` exports use `/api/calendar/[id]` and must be confirmed against a real platform after integration.
