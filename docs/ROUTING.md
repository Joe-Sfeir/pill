# Routing

Explicit routes own their metadata and content. Only class and instructor details are dynamic. Unknown slugs call `notFound()`. Root required/optional catch-alls are prohibited because they mask broken links and can conflict with explicit routes. No Pages Router exists.
