# Class data

`ClassType` in `types/index.ts` is authoritative. Add unique IDs/slugs, complete accessibility descriptions, honest intensity/level, generic equipment terminology, prerequisites, first-timer suitability, and only verified instructor IDs. `maximumCapacity: null` means unknown and must not appear live.
