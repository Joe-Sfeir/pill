export type Intensity = "Gentle" | "Moderate" | "Challenging";
export type Level = "Beginner" | "All levels" | "Experienced";
export type ClassType = { id:string; slug:string; name:string; shortDescription:string; fullDescription:string; duration:number; intensity:Intensity; level:Level; equipment:string[]; maximumCapacity:number|null; instructorIds:string[]; image:string; accent:string; prerequisites:string[]; firstTimerSuitable:boolean; active:boolean; expect:string[]; preparation:string[] };
export type Instructor = { id:string; slug:string; name:string; pronouns?:string; role:string; shortBio:string; fullBio:string; specialties:string[]; credentials:string[]; classIds:string[]; portrait:string; social?:string; active:boolean; teachingStyle:string; favoriteClass:string };
export type SessionStatus = "Available"|"Nearly full"|"Full"|"Waitlist"|"Cancelled";
export type Session = { id:string; classId:string; instructorId:string; date:string; startTime:string; endTime:string; timeZone:string; capacity:number|null; bookedCount:number|null; status:SessionStatus; waitlistStatus:"Open"|"Closed"|"Not applicable"; cancellationNote?:string; replacementInstructorId?:string; recurring:boolean; bookingUrl:string; firstTimerEligible:boolean; demonstration:true };
export type Package = { id:string; name:string; description:string; type:"Intro offer"|"Single class"|"Class package"|"Monthly membership"|"Private package"; billingPeriod?:string; sessions:number|null; validity:string; price:{USD:number|null;LBP:number|null}; autoRenewal:boolean; freezeRules:string; cancellationSummary:string; featured:boolean; active:boolean };
