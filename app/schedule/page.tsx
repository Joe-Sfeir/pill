import type {Metadata} from "next";import {PageHeader} from "@/components/ui/PageHeader";import {ScheduleList} from "@/components/schedule/ScheduleList";export const metadata:Metadata={title:"Schedule",description:"Filter the weekly FORME demonstration schedule."};export default function Page(){return <><PageHeader eyebrow="Asia / Beirut" title="The weekly rhythm." intro="Browse by class, instructor, and first-session eligibility. Live scheduling is not yet connected."/><section className="section container"><div className="actions" aria-label="Week navigation"><button className="btn outline" disabled>Previous week</button><button className="btn outline" aria-current="date">Current week</button><button className="btn outline" disabled>Next week</button></div><ScheduleList/></section></>}
