"use client";
import {useState} from "react";import Link from "next/link";import {packages} from "@/content/data";import {PageHeader} from "@/components/ui/PageHeader";import {studio} from "@/config/studio";export default function Page(){const [currency,setCurrency]=useState<"USD"|"LBP">("USD");return <><PageHeader eyebrow="Packages + memberships" title="Choose your cadence." intro="Every price and commercial term below is a configurable placeholder—not an offer for sale."/><section className="section container"><div className="filters" role="group" aria-label="Currency"><button className={`btn ${currency==="USD"?"":"outline"}`} onClick={()=>setCurrency("USD")}>USD</button><button className={`btn ${currency==="LBP"?"":"outline"}`} onClick={()=>setCurrency("LBP")}>LBP</button></div><p className="placeholder">No live exchange rate is configured. {currency} figures remain hidden until approved.</p><div className="index-grid">{packages.map(p=><article className="class-card" key={p.id}><div><span className="tag">{p.type}</span><h2>{p.name}</h2><p>{p.description}</p><strong>{p.price[currency]===null?`[${currency} price to confirm]`:p.price[currency]}</strong><p>{p.validity}</p><p>{p.cancellationSummary}</p></div><a href={`https://wa.me/${studio.whatsapp}?text=${encodeURIComponent(`Hello ${studio.shortName}, I’d like information about ${p.name}.`)}`} target="_blank" rel="noopener noreferrer">Ask on WhatsApp →</a></article>)}</div><div className="actions"><Link className="btn" href="/first-session">Book first session</Link><Link className="btn outline" href="/booking-policy">Booking policy</Link><Link className="btn outline" href="/cancellation-policy">Cancellation policy</Link></div></section></>}
